// initial answer

// left
if (answer === 'left') {
  answer = window.prompt('You see cats')
  // stay
    if (answer === 'stay') {

      // stay
      // spread the world
    }
  // continue
}
// right